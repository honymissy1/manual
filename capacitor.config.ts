import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.perfectionmanual.app',
  appName: 'Manual',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
